//npm is node package mamager and it is standard package manager for nodejs 

//it performs two tasks ie: 1. it is a library of packages 2. it is a command line tool through which developers can install, change the packages.

//installing npm packages : npm install <package> 

//paackages are always installed in the directory and the package is not accessible outiside the package.
//when a package is installed, there are 3 files created automatically 1. node_module 2.package-lock.json 3.package.json

//1. node_module contains all the dependencies related to the project 
//2. package-lock.json records the information related to all the dependencies in the project. 
//3. package.json contains all the information about the project => npm init
